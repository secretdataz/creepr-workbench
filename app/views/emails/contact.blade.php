<!DOCTYPE html>
<html lang="en-US">
	<head>
		<meta charset="utf-8">
	</head>
  <body>
    <h4>Message from {{ $username }}</h4>
    <p class="lead">{{ $subject }}</p>
		<p>{{ $email }}</p>
    <p>{{ $response }}</p>
  </body>
</html>
